
#? nos permiten guardar valores(las variables se crean y almacenan temporalmente en la memoria RAM cuando se ejecuta un programa.) pudiendo reutilizar en diferentes partes del codigo para crear variables tienes que segir esta forma:
#! darle un nombre(no lef, def, class, for, break...) | luego asignarles un valor
#! <variable name> = <valor>(hay varios tipos como:)

numero_1 = 10 #int

texto = 'A' #string

fin = False #booleano

#todo| Reglas:
#? 1.- las variable  pueden comenzar por letras o undercore:

xy = True
_y = False

#? 2.- pueden tener numeros y undercores:

variable_1 = 9

#? son case sensitive = distinge mayusculas y minusculas

x_ = 9
y = x + 5
